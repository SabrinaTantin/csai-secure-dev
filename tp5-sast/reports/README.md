# Déposer ici les rapports Bandit avant/après correction (TP5).

```bash
bandit -r sample_project -f txt -o reports/bandit-before.txt
# ... corrections ...
bandit -r sample_project -f txt -o reports/bandit-after.txt
```
